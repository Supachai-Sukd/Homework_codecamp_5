let age = prompt(`กรุณากรอกอายุ`);

(age >= 18 && age <= 60) && alert(`คุณมีอายุ ${age} ซึ่งอยู่ระหว่าง 18 - 60`);
(age < 18 || age > 60) && alert(`คุณมีอายุ ${age} อายุคุณไม่อยู่ระหว่าง 18 - 60`);


    
