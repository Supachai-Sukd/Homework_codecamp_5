function sumInput (sum) {
    let i,result;
    let total = [];
    while (sum !== typeof(Number)) {
        let read = prompt("Enter number");
        read = total[i]
        i++;
    }
    alert(result)
}